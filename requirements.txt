# Requirements for running the demo app (demo_app/app.py), including clineval's
# own dependencies — the app imports clineval directly from src/ via sys.path,
# so the package itself doesn't need to be separately installed to run the demo.
streamlit>=1.36
plotly>=5.20
pandas>=2.0
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
requests>=2.31
